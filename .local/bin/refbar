#!/usr/bin/env sh

# Refresh the dwmbar.
# Send SIGTRAP signal to dwmbar script, which will handle it with a trap.
#kill -SIGTRAP $(ps aux | grep 'sh' | grep 'dwmbar' | awk '{print $2}')
{
	kill -5 $(ps aux | grep 'sh' | grep 'dwmbar' | awk '{print $2}')
} || {
	dwmbar &
}
